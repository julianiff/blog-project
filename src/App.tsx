import React from "react";

import { Button } from "ls-button";
import { Card } from "ls-card";
import { Title } from "ls-title";
import { CurvedBg } from "ls-curved-bg";
import { Header } from "ls-header";

import "./App.css";
import { BlogOne } from "./blog-1";

function App() {
  return (
    <div className="App">
      <header className="App-header">
        This is a blank Page
        <Header></Header>
      </header>
    </div>
  );
}

export default App;
